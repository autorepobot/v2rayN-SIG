name: 🀫 Sync Upstream & Auto-Heal Patches

on:
  schedule:
    - cron: '0 3 * * *'

  workflow_dispatch:
    inputs:
      force_run:
        description: '是否强制运行补丁检测与 Gemini 自愈'
        required: false
        type: boolean
        default: true

permissions:
  contents: write
  pull-requests: write

jobs:
  check-and-heal-patches:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout self repo
        uses: actions/checkout@v7
        with:
          ref: master
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Check and Repair Patches
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          set -euo pipefail

          # ------------------------------------------------------------
          # Check API key
          # ------------------------------------------------------------
          if [[ -z "${GEMINI_API_KEY:-}" ]]; then
            echo "::error::GEMINI_API_KEY is not configured"
            exit 1
          fi

          # ------------------------------------------------------------
          # Git config
          # ------------------------------------------------------------
          git config user.email "actions@github.com"
          git config user.name "GitHubActions"

          # ------------------------------------------------------------
          # Upstream
          # ------------------------------------------------------------
          git remote remove upstream 2>/dev/null || true
          git remote add upstream https://github.com/2dust/v2rayN.git

          echo "Fetching upstream/master..."
          git fetch upstream master --depth=1

          UPSTREAM_COMMIT="$(git rev-parse upstream/master)"
          echo "Upstream HEAD: $UPSTREAM_COMMIT"

          # ------------------------------------------------------------
          # Find patches
          # ------------------------------------------------------------
          shopt -s nullglob
          PATCHES=(patch/*.patch patch/*.diff)
          shopt -u nullglob

          if (( ${#PATCHES[@]} == 0 )); then
            echo "No patches found."
            exit 0
          fi

          # ------------------------------------------------------------
          # Create clean worktree from upstream/master
          # ------------------------------------------------------------
          TMP_DIR="$(mktemp -d)"
          WORKTREE="$TMP_DIR/upstream"

          cleanup() {
            git worktree remove --force "$WORKTREE" 2>/dev/null || true
            rm -rf "$TMP_DIR"
          }

          trap cleanup EXIT

          echo "Creating clean upstream worktree..."
          git worktree add --detach "$WORKTREE" upstream/master

          if [[ -n "$(git -C "$WORKTREE" status --porcelain)" ]]; then
            echo "::error::Temporary upstream worktree is not clean"
            exit 1
          fi

          REPAIRED=0
          CONFLICTS=0

          # ------------------------------------------------------------
          # Process patches
          # ------------------------------------------------------------
          for PATCH in "${PATCHES[@]}"; do
            echo
            echo "============================================================"
            echo "Checking: $PATCH"
            echo "============================================================"

            # ----------------------------------------------------------
            # IMPORTANT:
            # Check against upstream/master, NOT self/master
            # ----------------------------------------------------------
            if git -C "$WORKTREE" apply --check --recount "$GITHUB_WORKSPACE/$PATCH"; then
              echo "✅ Clean: $PATCH"
              continue
            fi

            CONFLICTS=$((CONFLICTS + 1))

            ERROR="$(git -C "$WORKTREE" apply --check --recount "$GITHUB_WORKSPACE/$PATCH" 2>&1 || true)"

            echo "❌ Conflict detected:"
            echo "$ERROR"

            # ----------------------------------------------------------
            # Collect affected files
            # ----------------------------------------------------------
            FILES="$(
              grep -E '^(--- a/|\+\+\+ b/)' "$PATCH" |
              sed -E 's#^(--- a/|\+\+\+ b/)##' |
              grep -v '^/dev/null$' |
              sort -u
            )"

            if [[ -z "$FILES" ]]; then
              echo "::error::Could not detect target files in $PATCH"
              continue
            fi

            # ----------------------------------------------------------
            # Build upstream source context
            # ----------------------------------------------------------
            CONTEXT=""

            while IFS= read -r FILE; do
              [[ -z "$FILE" ]] && continue

              echo "Loading upstream: $FILE"

              if CONTENT="$(git show "upstream/master:$FILE" 2>/dev/null)"; then
                CONTEXT+="
### File: $FILE
\`\`\`
$CONTENT
\`\`\`
"
              else
                CONTEXT+="
### File: $FILE
(File missing in upstream)
"
              fi
            done <<< "$FILES"

            # ----------------------------------------------------------
            # Build Gemini prompt
            # ----------------------------------------------------------
            PATCH_CONTENT="$(cat "$PATCH")"

            PROMPT="$(cat <<EOF
You are an expert Git engineer.

A unified diff patch failed to apply to the latest upstream/master
of the v2rayN repository.

Upstream commit:
$UPSTREAM_COMMIT

Patch file:
$PATCH

Original patch:
\`\`\`diff
$PATCH_CONTENT
\`\`\`

git apply --check error:
\`\`\`
$ERROR
\`\`\`

Relevant files from latest upstream/master:
$CONTEXT

Instructions:
1. Reconstruct an updated unified diff that applies cleanly to latest upstream/master.
2. Preserve the original intent of the patch.
3. Do not introduce unrelated changes.
4. Output ONLY the unified diff.
EOF
)"

            # ----------------------------------------------------------
            # Call Gemini
            # ----------------------------------------------------------
            echo "🤖 Calling Gemini..."

            REQUEST="$(
              jq -n \
                --arg prompt "$PROMPT" \
                '{
                  contents: [
                    {
                      parts: [
                        {
                          text: $prompt
                        }
                      ]
                    }
                  ]
                }'
            )"

            RESPONSE="$(
              curl -fsS \
                -X POST \
                "https://generativelanguage.googleapis.com/v1beta/models/gemini-3.7-flash:generateContent?key=${GEMINI_API_KEY}" \
                -H "Content-Type: application/json" \
                -d "$REQUEST"
            )"

            # ----------------------------------------------------------
            # Extract Gemini text
            # ----------------------------------------------------------
            TEXT="$(
              jq -r '.candidates[0].content.parts[0].text // empty' \
                <<< "$RESPONSE"
            )"

            if [[ -z "$TEXT" ]]; then
              echo "::error::Gemini returned no usable response"
              echo "$RESPONSE"
              continue
            fi

            # ----------------------------------------------------------
            # Extract diff from markdown code block
            # ----------------------------------------------------------
            REPAIRED="$(
              printf '%s\n' "$TEXT" |
              sed -n '/^```\(diff\|patch\)\?$/,/^```$/p' |
              sed '1d;$d'
            )"

            if [[ -z "$REPAIRED" ]]; then
              echo "::error::Failed to parse Gemini patch"
              echo "$TEXT"
              continue
            fi

            # ----------------------------------------------------------
            # Validate Gemini patch BEFORE touching original
            # ----------------------------------------------------------
            CANDIDATE="$TMP_DIR/$(basename "$PATCH").gemini"

            printf '%s\n' "$REPAIRED" > "$CANDIDATE"

            echo "Validating Gemini-generated patch..."

            if ! VALIDATION_ERROR="$(
              git -C "$WORKTREE" apply --check --recount "$CANDIDATE" 2>&1
            )"; then
              echo "❌ Gemini repair failed validation:"
              echo "$VALIDATION_ERROR"
              echo "⚠️ Original patch was left untouched."
              continue
            fi

            # ----------------------------------------------------------
            # Only replace original AFTER validation succeeds
            # ----------------------------------------------------------
            cp "$CANDIDATE" "$PATCH"

            REPAIRED=$((REPAIRED + 1))

            echo "✨ Repaired: $PATCH"

          done

          # ------------------------------------------------------------
          # Summary
          # ------------------------------------------------------------
          echo
          echo "============================================================"
          echo "Summary"
          echo "============================================================"
          echo "Total patches : ${#PATCHES[@]}"
          echo "Conflicts     : $CONFLICTS"
          echo "Repaired      : $REPAIRED"

          if (( CONFLICTS == 0 )); then
            echo "✅ All patches apply cleanly to upstream/master."
          elif (( REPAIRED == CONFLICTS )); then
            echo "✅ All conflicts were successfully repaired."
          else
            echo "⚠️ Some conflicts could not be automatically repaired."
          fi

      - name: Show Changes
        run: |
          git status --short
          echo
          git diff -- patch/

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.PAT_TOKEN || secrets.GITHUB_TOKEN }}
          commit-message: "fix(patch): auto-heal patch conflicts"
          title: "🀫 [Auto-Fix] 自动修复 Patch 冲突"
          body: |
            ## 🀫 Auto-Healed Patch Conflicts

            Automatically repaired patches that no longer apply
            cleanly to upstream/master.

            Validation flow:

            1. Check patch against upstream/master
            2. Call Gemini only when the check fails
            3. Validate Gemini-generated patch
            4. Submit only validated repairs
          branch: "auto-fix/patch-conflicts"
          delete-branch: true
